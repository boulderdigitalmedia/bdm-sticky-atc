{
  "name": "bdm-sticky-atc",
  "private": true,
  "type": "module",
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "start": "npx prisma migrate deploy && node web/index.js",
    "build": "npm --prefix web/frontend install && npm --prefix web/frontend run build",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "@shopify/app-bridge": "^3.7.11",
    "@shopify/app-bridge-utils": "^3.5.1",
    "@shopify/shopify-api": "^12.3.0",
    "@shopify/shopify-app-express": "^6.0.5",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "dotenv-cli": "^7.4.2",
    "prisma": "^5.22.0"
  }
}
